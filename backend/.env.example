# Copy to .env and adjust for your environment.
# PostgreSQL connection for the emergency incident handoff service.
DATABASE_URL=postgres://huangding@localhost:5432/incident_handoff_gsb_0801_dev
# Separate database used by the automated test suite (it is TRUNCATEd between tests).
TEST_DATABASE_URL=postgres://huangding@localhost:5432/incident_handoff_gsb_0801_test
HOST=0.0.0.0
PORT=8080
# Comma separated list of allowed CORS origins for the Vite dev server.
CORS_ORIGIN=http://localhost:5173
